Limitations
###########

Strict modules undergo a number of different checks to ensure that they will
reliably and consistently produce the same module for the same input source
code. This ensures that top-level modules will always reliably succeed and won’t
take dependencies on external environmental factors.

.. toctree::
    :maxdepth: 1
    :titlesonly:
    :glob:

    *
